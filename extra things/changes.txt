def area_create():
    # wait.until(EC.visibility_of_element_located((By.XPATH, "//a[@href='/System/DashboardConfigCloud']")))
    wait.until(EC.element_to_be_clickable((By.XPATH, "//a[@href='/System/DashboardConfigCloud']"))).click()
    wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='Area Group Management']/parent::a"))).click()
    # wait.until(EC.element_to_be_clickable((By.XPATH,"//div[@role='listitem' and.//div[normalize-space()='NI01']/parent::div"))).click()
    company = "NI01"
    # # xpath = f"//div[normalize-space()='{company}']/ancestor::div[@role='listitem']"
    # # wait.until(EC.element_to_be_clickable((By.XPATH, xpath))).click()
    # wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='Add']/parent::button"))).click()

    xpath_company = f"""//h4[normalize-space()='COMPANY']/ancestor::div[contains(@class,'col')]//div[normalize-space()='{company}']"""
    wait.until(EC.element_to_be_clickable((By.XPATH, xpath_company))).click()
    xpath_add = f"""//h4[normalize-space()='COUNTRY']/ancestor::div[contains(@class,'col')]//button[.//span[normalize-space()='Add']]"""
    wait.until(EC.element_to_be_clickable((By.XPATH, xpath_add))).click()
    company_name = wait.until(EC.visibility_of_element_located((By.XPATH, "//input[@placeholder='Input the new country']")))
    company_name.send_keys("Norway")
    wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='Save']/parent::button"))).click()
    wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='OK']/parent::button"))).click()
    
    country = "Norway"
    xpath_country = f"""//h4[normalize-space()='COUNTRY']/ancestor::div[contains(@class,'col')]//div[normalize-space()='{country}']"""
    wait.until(EC.element_to_be_clickable((By.XPATH, xpath_country))).click()
    xpath_add = f"""//h4[normalize-space()='REGION']/ancestor::div[contains(@class,'col')]//button[.//span[normalize-space()='Add']]"""
    wait.until(EC.element_to_be_clickable((By.XPATH, xpath_add))).click()
    # xpath = f"//div[normalize-space()='{country}']/ancestor::div[@role='listitem']"
    # wait.until(EC.element_to_be_clickable((By.XPATH, xpath))).click()
    # wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='Add']/ancestor::button"))).click()
    region_name = wait.until(EC.visibility_of_element_located((By.XPATH, "//div//input[@placeholder='Input the new region']")))
    region_name.send_keys("Norwayy")
    wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='Save']/parent::button"))).click()
    wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='OK']/parent::button"))).click()

    region = "Norwayy"
    # xpath = f"//div[normalize-space()='{region}']/ancestor::div[@role='listitem']"
    # wait.until(EC.element_to_be_clickable((By.XPATH, xpath))).click()
    # wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='Add']/parent::button"))).click()

    xpath_region = f"""//h4[normalize-space()='REGION']/ancestor::div[contains(@class,'col')]//div[normalize-space()='{region}']"""
    wait.until(EC.element_to_be_clickable((By.XPATH, xpath_region))).click()
    xpath_add = f"""//h4[normalize-space()='CITY']/ancestor::div[contains(@class,'col')]//button[.//span[normalize-space()='Add']]"""
    wait.until(EC.element_to_be_clickable((By.XPATH, xpath_add))).click()
    city_name = wait.until(EC.visibility_of_element_located((By.XPATH, "//input[@placeholder='Input the new city']"))).click()
    city_name.send_keys("Norway")
    wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='Save']/parent::button"))).click()
    wait.until(EC.element_to_be_clickable((By.XPATH,"//span[normalize-space()='OK']/parent::button"))).click()


drop down

def select_dropdown(label, option):

    # Click dropdown using its label
    dropdown = wait.until(EC.element_to_be_clickable((
            By.XPATH,
            f"//label[normalize-space()='{label}']/ancestor::div[contains(@class,'v-input')]//div[@role='button']"
        )))

    dropdown.click()
    wait.until(
    EC.visibility_of_element_located((By.XPATH, "//div[contains(@class,'menuable__content__active')]")))

    time.sleep(1)

    # Wait for dropdown menu
    wait.until(
        EC.visibility_of_element_located(
            (By.XPATH, "//div[@role='listbox']")
        ) )

    while True:

        try:

            item = driver.find_element(
                By.XPATH,
                f"//div[@role='option']//div[normalize-space()='{option}']"
            )

            driver.execute_script(
                "arguments[0].scrollIntoView({block:'center'});",
                item
            )

            driver.execute_script("arguments[0].click();", item)

            break

        except NoSuchElementException:

            listbox = driver.find_element(
                By.XPATH,
                "//div[@role='listbox']"
            )

            driver.execute_script(
                "arguments[0].scrollTop += 250;",
                listbox
            )

            time.sleep(.3)


 the below is for the drop down in the esl to operate 

def open_dropdown(dropdown_id, option):
    dropdown = wait.until(
        EC.element_to_be_clickable((By.XPATH,f"//input[@id='{dropdown_id}']/ancestor::div[@role='button']")))

    driver.execute_script("arguments[0].scrollIntoView({block:'center'});", dropdown)
    dropdown.click()

    option = wait.until(EC.element_to_be_clickable((By.XPATH,"//div[@role='option'][.//div[normalize-space()='INDIA']]")))
    option.click()


upload product_search

    x=wait.until(EC.presence_of_element_located((By.XPATH,"//input[@placeholder='Input Product ID']")))
    x.click()
    x.send_keys("TR62")
    wait.until(EC.element_to_be_clickable((By.XPATH,"//Span[normalize-space()='Search']/parent::button"))).click()



way to find the exist of path 

    inputs = driver.find_elements(By.XPATH, "//input[@placeholder='Input Active Count Value(1~225, unit:1)']")
    print("Number of file inputs:", len(inputs))

        inputs = driver.find_elements(By.XPATH, "//label[normalize-space()='Button 1 (Main)']/ancestor::div[contains(@class,'v-input__control')]//div[@role='button']")
    print("Number of file inputs:", len(inputs))





    to check how many ways are there , and hoe many paths are it finding and is it hidden or not 

        print(x.get_attribute("outerHTML"))
    # options = driver.find_elements(By.XPATH,f"//div[@role='option'][.//div[normalize-space()='{Button_2}']]")

    # print("Found:", len(options))

    # for i, opt in enumerate(options):
    #     print(f"\nOption {i+1}")
    #     print("Displayed:", opt.is_displayed())
    #     print("Location:", opt.location)
    #     print(opt.get_attribute("outerHTML"))


    for the lael assign , the combobox extra tried things 

    # # Click the combobox first
#     # wait.until(EC.invisibility_of_element_located((By.XPATH, "//div[contains(@class,'v-overlay__scrim')]")))
#     # time.sleep(0.5)
# #     wait.until(EC.element_to_be_clickable((By.XPATH,"//div[@role='combobox']"))).click()
# # # Now type into the input
# #     time.sleep(1)
# #     x = wait.until(EC.visibility_of_element_located((By.XPATH,"//div[@role='combobox']//input")))
# #     x.send_keys(product)
#     options = driver.find_elements(By.XPATH,f"//input[@placeholder='Input The Label Code']")
#     print("Found:", len(options))

#     for i, opt in enumerate(options):
#         print(f"\nOption {i+1}")
#         print("Displayed:", opt.is_displayed())
#         print("Location:", opt.location)
#         print(opt.get_attribute("outerHTML"))
#     # x=wait.until(EC.element_to_be_clickable((By.CSS_SELECTOR,"input[placeholder='Input The ProductList']")))
#     # x.click()
#     # time.sleep(1)
#     # x.send_keys(product)


------------------------------------------------

    # print(x.get_attribute("outerHTML"))
    # options = driver.find_element(By.XPATH,f"//div[contains(@class,'v-application--wrap')]//input[@placeholder='Input The ProductList']")

    # print(options.get_attribute("outerHTML"))
---------------------------------------------------------
    # print("Found:", len(options))
    # for i, opt in enumerate(options):
    #     print(f"\nOption {i+1}")
    #     print("Displayed:", opt.is_displayed())
    #     print("Location:", opt.location)
    #     print(opt.get_attribute("outerHTML"))
    # x = wait.until(EC.presence_of_element_located((By.XPATH, "//input[placeholder='Input The ProductList']")))
    # x.send_keys(product)
--------------------------------------

to check the attributes if present 
state = driver.find_element(By.XPATH,"//label[normalize-space()='Template Types by Label']/preceding-sibling::div//input[@role='checkbox']"
).get_attribute("aria-checked")

if state == "false":
    checkbox.click()
----------------------------------------
 options = driver.find_elements(By.XPATH,f"//div[@role='option'][.//div[contains(normalize-space(), '{template_name}')]]")
    print("Found:", len(options))

    for i, opt in enumerate(options):
        print(f"\nOption {i+1}")
        print("Displayed:", opt.is_displayed())
        print("Location:", opt.location)
        print(opt.get_attribute("outerHTML"))
------------------------------------------------------
# def scroll_until_store(driver,Find, max_scrolls=30):

#     scroll_box = driver.find_element(By.XPATH,
#         "//div[contains(@class,'ps') and contains(@class,'ps--active-y')]")

#     for _ in range(max_scrolls):

#         try:
#             store = driver.find_element(By.XPATH,f"//div[@class='v-list-item__title' and normalize-space()='{Find}']")
#             return store

#         except NoSuchElementException:
#             driver.execute_script("arguments[0].scrollTop += 120;",scroll_box)
#             time.sleep(0.2)

#     raise Exception(f"{store_name} not found")